apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: test
  namespace: test

spec:
  rules:
    - host:
      http:
        paths:
          - path: /
            backend:
              serviceName: department-service
              servicePort: 80
